# 🧪 MLflow Tracking Lab

## 📚 บทนำ

Lab นี้จะพานักศึกษาเรียนรู้การใช้งาน **MLflow Tracking** ซึ่งเป็นเครื่องมือสำคัญในการจัดการและติดตาม Machine Learning Experiments อย่างเป็นระบบ

---

## 🎯 วัตถุประสงค์การเรียนรู้

เมื่อจบ Lab นี้ นักศึกษาจะสามารถ:

1. เข้าใจแนวคิดและองค์ประกอบหลักของ MLflow Tracking
2. บันทึก Parameters, Metrics และ Artifacts ได้
3. เปรียบเทียบผลการทดลองหลายๆ Runs
4. ใช้ MLflow UI ในการวิเคราะห์ผลการทดลอง
5. จัดการ Experiments และ Runs อย่างเป็นระบบ

---

## 📖 ทฤษฎีพื้นฐาน MLflow Tracking

### 🔍 MLflow คืออะไร?

**MLflow** เป็น Open Source Platform สำหรับจัดการ Machine Learning Lifecycle ทั้งหมด พัฒนาโดย Databricks และเปิดตัวครั้งแรกในปี 2018 ปัจจุบันเป็นหนึ่งในเครื่องมือ MLOps ที่ได้รับความนิยมมากที่สุด โดยมีจุดเด่นคือความยืดหยุ่นสูง รองรับหลายภาษาโปรแกรม (Python, R, Java, REST API) และสามารถทำงานร่วมกับ ML Framework ยอดนิยมได้ทุกตัว

#### ปัญหาที่ MLflow แก้ไข

ในการพัฒนา Machine Learning แบบดั้งเดิม Data Scientists มักประสบปัญหาหลายประการ:

1. **Reproducibility Crisis**: ไม่สามารถทำซ้ำผลการทดลองได้เพราะลืมว่าใช้ hyperparameters อะไร ใช้ data version ไหน หรือใช้ code version ใด
2. **Experiment Chaos**: มี notebook หลายสิบไฟล์ที่ชื่อคล้ายกัน เช่น `model_v1.ipynb`, `model_v2_final.ipynb`, `model_v2_final_real.ipynb` จนไม่รู้ว่าอันไหนดีที่สุด
3. **Collaboration Difficulty**: ทีมงานไม่สามารถแชร์และเปรียบเทียบผลการทดลองได้อย่างมีประสิทธิภาพ
4. **Deployment Gap**: การนำ Model ไป Deploy ต้องเขียน code ใหม่เกือบทั้งหมด

MLflow ถูกออกแบบมาเพื่อแก้ไขปัญหาเหล่านี้โดยเฉพาะ

#### องค์ประกอบหลัก 4 ส่วนของ MLflow Platform

MLflow ประกอบด้วย 4 Components ที่ทำงานร่วมกันครอบคลุม ML Lifecycle ทั้งหมด:

##### 📋 ตารางสรุป MLflow Components

| Component | Icon | Tagline | หน้าที่หลัก | ใช้เมื่อไหร่? |
|-----------|------|---------|-------------|---------------|
| **Tracking** | 📊 | "บันทึกทุกการทดลอง" | บันทึก Parameters, Metrics, Artifacts | ตอน Train/Experiment |
| **Projects** | 📦 | "รันที่ไหนก็ได้ผลเหมือนกัน" | Package code ให้ reproduce ได้ | ตอนแชร์ code กับทีม |
| **Models** | 🤖 | "Train ครั้งเดียว Deploy ได้ทุกที่" | Package model ให้ deploy ได้หลายที่ | ตอน Deploy model |
| **Registry** | 📋 | "จัดการ Model เหมือน Git" | Version control สำหรับ models | ตอนจัดการ Production |

---

##### 🔄 ML Lifecycle Flow

```
┌────────────────────────────────────────────────────────────────────────────────┐
│                              ML Lifecycle Flow                                 │
│                                                                                │
│    ┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│    │  📊 TRACKING │ ──▶ │  📦 PROJECTS │ ──▶ │  🤖 MODELS   │ ──▶ │  📋 REGISTRY │
│    │              │     │              │     │              │     │              │
│    │  ทดลอง &     │     │  Package     │     │  Deploy      │     │  จัดการ      │
│    │  บันทึกผล    │     │  Code        │     │  Model       │     │  Version     │
│    └──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
│                                                                                │
│    "ทดลองจนได้      →   "แพ็คโค้ดให้     →   "นำ Model ไป   →   "จัดการ Model   │
│     Model ที่ดี"         ทีมรันซ้ำได้"        ใช้งานจริง"        หลาย Versions" │
│                                                                                │
└────────────────────────────────────────────────────────────────────────────────┘
```

---

##### 📊 เปรียบเทียบ Components ทั้ง 4

| หัวข้อ | 📊 Tracking | 📦 Projects | 🤖 Models | 📋 Registry |
|--------|-------------|-------------|-----------|-------------|
| **บันทึกอะไร?** | Parameters, Metrics, Artifacts | Code, Dependencies, Entry points | Trained model, Model format | Model versions, Stages |
| **Output** | Runs, Experiments | Reproducible package | Deployable model | Versioned model store |
| **ไฟล์สำคัญ** | `mlruns/` folder | `MLproject`, `conda.yaml` | `MLmodel`, `model.pkl` | Registry database |
| **คำสั่งหลัก** | `mlflow.log_*()` | `mlflow run` | `mlflow.*.log_model()` | `mlflow.register_model()` |
| **UI** | Experiment UI | - | Model UI | Registry UI |

---

##### 🎯 เลือกใช้ Component ไหนดี?

| สถานการณ์ | Component ที่ใช้ | เหตุผล |
|-----------|-----------------|--------|
| กำลังทดลอง hyperparameters หลายค่า | 📊 **Tracking** | บันทึกและเปรียบเทียบผลได้ |
| ต้องการให้เพื่อนร่วมทีมรัน code เดียวกัน | 📦 **Projects** | มี dependencies ครบ รันได้ผลเหมือนกัน |
| จะนำ model ไป deploy บน server | 🤖 **Models** | Package model ให้พร้อม serve |
| มี model หลาย versions ต้องจัดการ | 📋 **Registry** | ติดตาม version, จัดการ Production |
| ทำทุกอย่างครบวงจร | **ใช้ทั้ง 4** | ครอบคลุม ML Lifecycle |

---

##### 💡 ตัวอย่าง Use Case จริง

| ขั้นตอน | สิ่งที่ทำ | Component |
|---------|----------|-----------|
| 1 | Data Scientist ทดลอง model หลายแบบ บันทึก accuracy แต่ละแบบ | 📊 Tracking |
| 2 | เลือก model ที่ดีที่สุด แพ็คโค้ดให้ทีม ML Engineer | 📦 Projects |
| 3 | ML Engineer นำ model ไป deploy เป็น REST API | 🤖 Models |
| 4 | เมื่อมี model ใหม่ที่ดีกว่า เปลี่ยน Production version | 📋 Registry |

---

> 📝 **สำหรับ Lab นี้**: เราจะเน้นที่ **📊 MLflow Tracking** เป็นหลัก เพราะเป็น component พื้นฐานที่สำคัญที่สุดและใช้บ่อยที่สุด

---

### 📊 รายละเอียด MLflow Tracking

#### 📊 1. MLflow Tracking (เน้นใน Lab นี้)

> **"บันทึกทุกการทดลอง เปรียบเทียบได้ทุกผลลัพธ์"**

MLflow Tracking คือระบบบันทึกและติดตามการทดลอง Machine Learning ช่วยให้คุณไม่ต้องจำว่าใช้ hyperparameters อะไร ได้ผลเท่าไหร่

**สิ่งที่บันทึกได้:**

| ประเภท | คำอธิบาย | ตัวอย่าง |
|--------|----------|----------|
| **Parameters** | ค่าที่ตั้งก่อนทดลอง (Input) | `learning_rate=0.001`, `epochs=100` |
| **Metrics** | ค่าที่วัดได้จากการทดลอง (Output) | `accuracy=0.95`, `loss=0.05` |
| **Artifacts** | ไฟล์ที่สร้างจากการทดลอง | `model.pkl`, `confusion_matrix.png` |
| **Tags** | ข้อมูลเพิ่มเติมสำหรับจัดกลุ่ม | `developer=john`, `env=production` |

**ตัวอย่างการใช้งาน:**
```python
import mlflow

mlflow.set_experiment("my-experiment")

with mlflow.start_run():
    mlflow.log_param("learning_rate", 0.001)    # บันทึก Parameter
    mlflow.log_metric("accuracy", 0.95)         # บันทึก Metric
    mlflow.log_artifact("model.pkl")            # บันทึก Artifact
```

---

### 📦 รายละเอียด MLflow Projects

#### 📦 2. MLflow Projects

> **"เขียนครั้งเดียว รันที่ไหนก็ได้ผลเหมือนกัน"**

MLflow Projects คือ standard format สำหรับ packaging ML code ให้สามารถ reproduce ได้

**องค์ประกอบหลัก:**

```
my_project/
├── MLproject              # ไฟล์กำหนดค่า entry points
├── conda.yaml             # Dependencies (conda)
├── requirements.txt       # Dependencies (pip)
└── train.py               # โค้ดหลัก
```

**ตัวอย่าง MLproject file:**
```yaml
name: my_ml_project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      learning_rate: {type: float, default: 0.01}
      epochs: {type: int, default: 100}
    command: "python train.py --lr {learning_rate} --epochs {epochs}"
```

**การรัน Project:**
```bash
# รันจาก local
mlflow run ./my_project -P learning_rate=0.001

# รันจาก GitHub
mlflow run git@github.com:user/repo.git -P epochs=50
```

---

### 🤖 รายละเอียด MLflow Models

#### 🤖 3. MLflow Models

> **"Train ครั้งเดียว Deploy ได้ทุกที่"**

MLflow Models คือ standard format สำหรับ packaging trained model ให้ deploy ได้หลาย platforms

**Model Flavors ที่รองรับ:**

| Category | Flavors |
|----------|---------|
| **Traditional ML** | sklearn, xgboost, lightgbm, catboost |
| **Deep Learning** | pytorch, tensorflow, keras |
| **Big Data** | spark, h2o |
| **Other** | onnx, prophet, statsmodels, custom |

**Deployment Targets:**

| Target | คำอธิบาย |
|--------|----------|
| **Local** | รัน prediction บนเครื่อง |
| **REST API** | Deploy เป็น REST endpoint |
| **Docker** | Package เป็น Docker container |
| **AWS SageMaker** | Deploy บน Amazon SageMaker |
| **Azure ML** | Deploy บน Azure Machine Learning |
| **Spark** | รันบน Apache Spark cluster |

**ตัวอย่างการบันทึกและ Deploy Model:**
```python
# บันทึก Model
import mlflow.sklearn
mlflow.sklearn.log_model(model, "my_model")

# Deploy เป็น REST API
# mlflow models serve -m runs:/<run_id>/my_model -p 5000
```

---

### 📋 รายละเอียด MLflow Model Registry

> **"จัดการ Model เหมือนจัดการ Code ด้วย Git"**

Model Registry คือ centralized store สำหรับจัดการ model versioning และ lifecycle เปรียบเทียบได้กับ Git ที่ใช้จัดการ source code

---

##### 🤔 ทำไมต้องมี Model Registry?

**เปรียบเทียบ: ไม่มี vs มี Model Registry**

| คำถามที่พบบ่อย | 😰 ไม่มี Model Registry | 😊 มี Model Registry |
|----------------|------------------------|---------------------|
| **"Model ที่รันบน Production คือไฟล์ไหน?"** | ไม่รู้... น่าจะเป็น `model_final_v2_new.pkl`? | `FraudDetector` version 3, stage: Production ✅ |
| **"Model นี้ train ด้วย hyperparameters อะไร?"** | จำไม่ได้... ลองหาใน notebook ดู | คลิกดู lineage → Run `abc123`, lr=0.001, epochs=100 ✅ |
| **"Model เมื่อวานดีกว่า rollback ได้ไหม?"** | ไม่ได้... ลบไฟล์เก่าไปแล้ว 😭 | ได้เลย! เปลี่ยน version 2 เป็น Production ✅ |
| **"ใครเป็นคน deploy model นี้? เมื่อไหร่?"** | ไม่รู้... ไม่มี log | `john@company.com`, 2024-01-15 14:30:00 ✅ |
| **"Model version ไหนดีที่สุด?"** | ต้องเปิด notebook เทียบเอง | ดูใน Registry UI เปรียบเทียบ metrics ได้เลย ✅ |
| **"ทีม QA จะทดสอบ model ใหม่ได้ยังไง?"** | ส่งไฟล์ให้ทาง Slack/Email | เปลี่ยน stage เป็น Staging แล้วแจ้งทีม ✅ |

---

##### 🔄 เปรียบเทียบ Model Registry กับ Git

| แนวคิด | Git (Code) | Model Registry (Model) |
|--------|------------|------------------------|
| **Repository** | Git Repository | Registered Model |
| **Version** | Commit | Model Version |
| **Branch/Tag** | main, develop, v1.0 | Production, Staging, Archived |
| **History** | Commit History | Version History |
| **Rollback** | `git checkout <commit>` | Transition to previous version |
| **Collaboration** | Pull Request | Stage Transition + Approval |
| **Metadata** | Commit Message | Description, Tags, Lineage |

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Git vs Model Registry                                    │
├─────────────────────────────────┬───────────────────────────────────────────┤
│            Git                  │           Model Registry                  │
├─────────────────────────────────┼───────────────────────────────────────────┤
│                                 │                                           │
│   Repository: my-app            │   Registered Model: FraudDetector         │
│   ├── commit abc123 (v1.0)      │   ├── Version 1 (Archived)                │
│   ├── commit def456 (v1.1)      │   ├── Version 2 (Staging)                 │
│   └── commit ghi789 (latest)    │   └── Version 3 (Production) ← Current   │
│            │                    │            │                              │
│            ▼                    │            ▼                              │
│   Branches:                     │   Stages:                                 │
│   • main (production)           │   • Production (ใช้งานจริง)               │
│   • develop (testing)           │   • Staging (ทดสอบ)                       │
│   • feature/* (development)     │   • Archived (เก็บถาวร)                   │
│                                 │                                           │
└─────────────────────────────────┴───────────────────────────────────────────┘
```

---

##### 🏷️ Model Stages อธิบายละเอียด

Model Registry มี 4 Stages สำหรับจัดการ lifecycle ของ model:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Model Lifecycle Stages                              │
│                                                                             │
│                                                                             │
│    ┌──────────────┐                                                         │
│    │     None     │  ← Model ที่เพิ่งลงทะเบียน ยังไม่ได้กำหนด stage         │
│    │  (ค่าเริ่มต้น) │                                                         │
│    └──────┬───────┘                                                         │
│           │                                                                 │
│           │ เริ่มทดสอบ                                                      │
│           ▼                                                                 │
│    ┌──────────────┐                                                         │
│    │   Staging    │  ← Model กำลังถูกทดสอบ (QA, Integration Test)           │
│    │  (ทดสอบ)     │    เช่น ทดสอบกับข้อมูลจริง, ทดสอบ performance           │
│    └──────┬───────┘                                                         │
│           │                                                                 │
│           │ ผ่านการทดสอบ ✅                                                  │
│           ▼                                                                 │
│    ┌──────────────┐                                                         │
│    │  Production  │  ← Model ที่ใช้งานจริง serve ให้ users                  │
│    │ (ใช้งานจริง)  │    ควรมีแค่ 1 version ต่อ 1 Registered Model            │
│    └──────┬───────┘                                                         │
│           │                                                                 │
│           │ มี version ใหม่มาแทน / เลิกใช้                                   │
│           ▼                                                                 │
│    ┌──────────────┐                                                         │
│    │   Archived   │  ← Model ที่เลิกใช้แล้ว เก็บไว้อ้างอิง/rollback          │
│    │ (เก็บถาวร)    │    ไม่ลบทิ้ง เผื่อต้องการกลับมาใช้                       │
│    └──────────────┘                                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**สรุป Stage แต่ละประเภท:**

| Stage | สัญลักษณ์ | คำอธิบาย | ใครใช้? |
|-------|----------|----------|---------|
| **None** | 🆕 | เพิ่งลงทะเบียน ยังไม่พร้อม | Data Scientist (กำลังพัฒนา) |
| **Staging** | 🧪 | กำลังทดสอบ อาจมีหลาย versions | QA Team, ML Engineer |
| **Production** | 🚀 | ใช้งานจริง ควรมี 1 version | End Users, Applications |
| **Archived** | 📦 | เลิกใช้แล้ว เก็บไว้อ้างอิง | ไม่มีใครใช้ (backup) |

---

##### 📊 ตัวอย่าง Workflow จริง

สมมติทีมพัฒนา Fraud Detection Model:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Fraud Detection Model - Version History                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  📋 Registered Model: "FraudDetector"                                       │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ Version 1                                                           │    │
│  │ ├── Stage: Archived 📦                                              │    │
│  │ ├── Created: 2024-01-01 by john@company.com                         │    │
│  │ ├── Source: runs:/abc111/model                                      │    │
│  │ ├── Metrics: accuracy=0.85, f1=0.82                                 │    │
│  │ └── Note: "Baseline model with LogisticRegression"                  │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                              │                                              │
│                              │ ถูกแทนที่โดย Version 2                       │
│                              ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ Version 2                                                           │    │
│  │ ├── Stage: Archived 📦                                              │    │
│  │ ├── Created: 2024-01-15 by jane@company.com                         │    │
│  │ ├── Source: runs:/abc222/model                                      │    │
│  │ ├── Metrics: accuracy=0.91, f1=0.89                                 │    │
│  │ └── Note: "Improved with RandomForest + feature engineering"        │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                              │                                              │
│                              │ ถูกแทนที่โดย Version 3                       │
│                              ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ Version 3                                                  🚀       │    │
│  │ ├── Stage: Production                                               │    │
│  │ ├── Created: 2024-02-01 by jane@company.com                         │    │
│  │ ├── Source: runs:/abc333/model                                      │    │
│  │ ├── Metrics: accuracy=0.95, f1=0.93                                 │    │
│  │ └── Note: "XGBoost with hyperparameter tuning - BEST"               │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                              │                                              │
│                              │ กำลังพัฒนา Version 4                         │
│                              ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ Version 4                                                  🧪       │    │
│  │ ├── Stage: Staging                                                  │    │
│  │ ├── Created: 2024-02-15 by john@company.com                         │    │
│  │ ├── Source: runs:/abc444/model                                      │    │
│  │ ├── Metrics: accuracy=0.96, f1=0.94                                 │    │
│  │ └── Note: "Testing Deep Learning approach"                          │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Workflow อธิบาย:**

```
วันที่ 1: John สร้าง Version 1 (Baseline)
    → ลงทะเบียนเป็น FraudDetector Version 1
    → ทดสอบผ่าน → เปลี่ยนเป็น Production

วันที่ 15: Jane สร้าง Version 2 (ดีกว่า)
    → ลงทะเบียนเป็น FraudDetector Version 2
    → ทดสอบใน Staging
    → ผ่านการทดสอบ → เปลี่ยนเป็น Production
    → Version 1 ถูกเปลี่ยนเป็น Archived

วันที่ 30: Jane สร้าง Version 3 (ดีที่สุด)
    → ลงทะเบียน → ทดสอบ → Production
    → Version 2 → Archived

วันที่ 45: John กำลังทดสอบ Version 4
    → อยู่ใน Staging รอทดสอบ
    → ถ้าผ่าน จะขึ้น Production แทน Version 3
```

---

##### 🔧 ตัวอย่าง Code การใช้งาน Model Registry

```python
import mlflow
from mlflow.tracking import MlflowClient

# สร้าง Client สำหรับจัดการ Registry
client = MlflowClient()

# ============================================
# 1. ลงทะเบียน Model ใหม่
# ============================================
# หลังจาก train model เสร็จใน MLflow Run
model_uri = "runs:/abc123def456/model"
model_name = "FraudDetector"

# ลงทะเบียน (ถ้ายังไม่มี จะสร้าง Registered Model ใหม่)
result = mlflow.register_model(model_uri, model_name)
print(f"Registered: {result.name} version {result.version}")
# Output: Registered: FraudDetector version 1

# ============================================
# 2. เพิ่มคำอธิบายให้ Model Version
# ============================================
client.update_model_version(
    name="FraudDetector",
    version=1,
    description="Baseline model using LogisticRegression. Accuracy: 85%"
)

# ============================================
# 3. เปลี่ยน Stage
# ============================================
# ย้ายไป Staging เพื่อทดสอบ
client.transition_model_version_stage(
    name="FraudDetector",
    version=1,
    stage="Staging"
)
print("Version 1 → Staging 🧪")

# ทดสอบผ่านแล้ว ย้ายไป Production
client.transition_model_version_stage(
    name="FraudDetector",
    version=1,
    stage="Production"
)
print("Version 1 → Production 🚀")

# ============================================
# 4. โหลด Model จาก Registry
# ============================================
# โหลดจาก Production (แนะนำ - ได้ model ล่าสุดที่ใช้งานจริง)
model = mlflow.pyfunc.load_model("models:/FraudDetector/Production")

# โหลดจาก version ที่ระบุ
model_v1 = mlflow.pyfunc.load_model("models:/FraudDetector/1")

# โหลดจาก Staging
model_staging = mlflow.pyfunc.load_model("models:/FraudDetector/Staging")

# ============================================
# 5. ใช้ Model ทำ Prediction
# ============================================
import pandas as pd

new_data = pd.DataFrame({
    "amount": [1000, 50000],
    "merchant": ["grocery", "electronics"]
})

predictions = model.predict(new_data)
print(f"Predictions: {predictions}")
# Output: Predictions: [0, 1]  (0=ปกติ, 1=fraud)

# ============================================
# 6. Rollback เมื่อมีปัญหา
# ============================================
# สมมติ Version 2 มีปัญหา ต้องกลับไปใช้ Version 1

# ย้าย Version 2 ออกจาก Production
client.transition_model_version_stage(
    name="FraudDetector",
    version=2,
    stage="Archived"
)

# ย้าย Version 1 กลับมา Production
client.transition_model_version_stage(
    name="FraudDetector",
    version=1,
    stage="Production"
)
print("Rollback complete! Version 1 is now Production 🔄")

# ============================================
# 7. ดูรายการ Model ทั้งหมด
# ============================================
# ดู Registered Models ทั้งหมด
for rm in client.search_registered_models():
    print(f"Model: {rm.name}")
    
# ดู Versions ทั้งหมดของ Model
for mv in client.search_model_versions("name='FraudDetector'"):
    print(f"  Version {mv.version}: {mv.current_stage}")
```

---

##### 📌 Model URI Formats

การอ้างอิง Model ใน MLflow มีหลายรูปแบบ:

| Format | ตัวอย่าง | ใช้เมื่อ |
|--------|----------|----------|
| **runs:/** | `runs:/abc123/model` | อ้างอิงจาก Run ID |
| **models:/{name}/{version}** | `models:/FraudDetector/1` | อ้างอิง version ที่ระบุ |
| **models:/{name}/{stage}** | `models:/FraudDetector/Production` | อ้างอิงจาก stage (แนะนำ) |
| **models:/{name}/latest** | `models:/FraudDetector/latest` | version ล่าสุด |

**แนะนำ:** ใช้ `models:/ModelName/Production` ในการ deploy เพราะ:
- ไม่ต้องแก้ code เมื่อเปลี่ยน version
- อัพเดท model แค่เปลี่ยน stage ใน Registry
- Application จะใช้ model ใหม่อัตโนมัติ

---

##### ✅ ประโยชน์ของ Model Registry (สรุป)

| ประโยชน์ | คำอธิบาย | ตัวอย่าง |
|----------|----------|----------|
| **🔢 Version Control** | ติดตามทุก version ไม่หายไปไหน | เก็บ Version 1, 2, 3, ... ครบถ้วน |
| **🏷️ Stage Management** | รู้สถานะของทุก model | Version 3 = Production, Version 4 = Staging |
| **🔗 Lineage Tracking** | รู้ที่มาที่ไปของ model | Model นี้มาจาก Run ไหน ใช้ data อะไร |
| **👥 Collaboration** | ทีมทำงานร่วมกันได้ | John ทดสอบ Staging, Jane approve Production |
| **⏪ Easy Rollback** | กลับ version เก่าได้ทันที | มีปัญหา? กลับ Version 2 ใน 1 นาที |
| **📝 Audit Trail** | มี log ครบทุกการเปลี่ยนแปลง | ใคร deploy อะไร เมื่อไหร่ |
| **🚀 Easy Deployment** | Deploy ง่าย แค่อ้างอิง stage | `models:/MyModel/Production` |

---

**ตัวอย่างการใช้งาน:**
```python
import mlflow

# ลงทะเบียน Model ใหม่
mlflow.register_model("runs:/<run_id>/model", "MyModel")

# เปลี่ยน Stage เป็น Production
client = mlflow.tracking.MlflowClient()
client.transition_model_version_stage(
    name="MyModel",
    version=1,
    stage="Production"
)

# โหลด Model จาก Production
model = mlflow.pyfunc.load_model("models:/MyModel/Production")
```

---

> 📝 **หมายเหตุ**: Lab นี้จะเน้นที่ **MLflow Tracking** เป็นหลัก เพราะเป็น component พื้นฐานที่สำคัญที่สุด ส่วน Model Registry จะเรียนรู้ใน Lab ถัดไป

---

### 🏗️ องค์ประกอบหลักของ MLflow Tracking

MLflow Tracking มีองค์ประกอบสำคัญ 5 ส่วนที่ทำงานร่วมกันเป็นระบบ:

#### 1️⃣ Experiment (การทดลอง)

**Experiment** คือกลุ่มของ Runs ที่เกี่ยวข้องกัน เปรียบเสมือน "โปรเจกต์" หรือ "หัวข้อการทดลอง" ทุก Run ต้องอยู่ภายใต้ Experiment ใดเสมอ

**หลักการตั้งชื่อ Experiment:**
- ใช้ชื่อที่สื่อความหมายชัดเจน เช่น `fraud-detection-lstm`, `customer-churn-prediction`
- หลีกเลี่ยงชื่อทั่วไป เช่น `test`, `experiment1`
- ใช้ kebab-case หรือ snake_case เพื่อความสม่ำเสมอ

```
Experiment: "Image Classification"
├── Run 1: ทดลองด้วย ResNet50
├── Run 2: ทดลองด้วย VGG16
├── Run 3: ทดลองด้วย EfficientNet
└── Run 4: ทดลองด้วย MobileNet
```

**ตัวอย่างการใช้งาน:**
```python
import mlflow

# สร้างหรือเลือก Experiment
# ถ้า Experiment นี้มีอยู่แล้ว จะเลือกใช้อันเดิม
# ถ้ายังไม่มี จะสร้างใหม่อัตโนมัติ
mlflow.set_experiment("image-classification")

# ดู Experiment ID ที่กำลังใช้อยู่
experiment = mlflow.get_experiment_by_name("image-classification")
print(f"Experiment ID: {experiment.experiment_id}")
print(f"Artifact Location: {experiment.artifact_location}")
```

**Experiment Properties:**
| Property | คำอธิบาย |
|----------|----------|
| `experiment_id` | Unique identifier (auto-generated) |
| `name` | ชื่อ Experiment ที่ตั้ง |
| `artifact_location` | ที่เก็บ Artifacts |
| `lifecycle_stage` | `active` หรือ `deleted` |
| `tags` | Metadata เพิ่มเติม |

#### 2️⃣ Run (การรันทดลอง)

**Run** คือการทดลองแต่ละครั้ง เป็นหน่วยพื้นฐานที่สุดของ MLflow Tracking ทุกครั้งที่ train model ด้วย hyperparameters ชุดใหม่ ควรสร้าง Run ใหม่

**Run ประกอบด้วย:**
- **Parameters**: ค่าที่ตั้งก่อนเริ่ม (input)
- **Metrics**: ค่าที่วัดได้ (output)
- **Artifacts**: ไฟล์ที่สร้างขึ้น
- **Metadata**: เวลาเริ่ม, เวลาจบ, สถานะ, Git commit hash ฯลฯ
- **Tags**: ข้อมูลเพิ่มเติมสำหรับการค้นหาและจัดกลุ่ม

**รูปแบบการสร้าง Run:**

```python
# แบบที่ 1: ใช้ Context Manager (แนะนำ)
# Run จะปิดอัตโนมัติเมื่อออกจาก with block
with mlflow.start_run(run_name="resnet50-experiment"):
    # โค้ดการทดลองอยู่ตรงนี้
    mlflow.log_param("learning_rate", 0.001)
    mlflow.log_metric("accuracy", 0.95)

# แบบที่ 2: เปิด-ปิดเอง (ใช้ในกรณีพิเศษ)
run = mlflow.start_run(run_name="manual-run")
try:
    mlflow.log_param("batch_size", 32)
finally:
    mlflow.end_run()

# แบบที่ 3: Nested Runs (สำหรับ Hyperparameter Tuning)
with mlflow.start_run(run_name="parent-run"):
    for lr in [0.001, 0.01, 0.1]:
        with mlflow.start_run(run_name=f"child-lr-{lr}", nested=True):
            mlflow.log_param("learning_rate", lr)
```

**Run Lifecycle States:**
```
┌─────────┐     ┌─────────┐     ┌──────────┐
│ RUNNING │────▶│ FINISHED│     │  FAILED  │
└─────────┘     └─────────┘     └──────────┘
     │                               ▲
     └───────────────────────────────┘
            (เมื่อเกิด exception)
```

#### 3️⃣ Parameters (พารามิเตอร์)

**Parameters** คือค่าที่เราตั้งก่อนเริ่มการทดลอง (Input Configuration) เป็นค่าคงที่ตลอดการทดลอง ไม่เปลี่ยนแปลง

**ประเภทของ Parameters:**

| ประเภท | ตัวอย่าง | คำอธิบาย |
|--------|----------|----------|
| **Model Hyperparameters** | learning_rate, batch_size, epochs, dropout | ค่าที่ควบคุมพฤติกรรมการเรียนรู้ของ Model |
| **Data Parameters** | train_split, validation_split, image_size, augmentation | ค่าที่เกี่ยวกับการเตรียมข้อมูล |
| **Architecture** | model_type, num_layers, hidden_units, activation | โครงสร้างของ Model |
| **Training Config** | optimizer, loss_function, early_stopping_patience | การตั้งค่าการ Train |
| **Environment** | random_seed, gpu_id, num_workers | การตั้งค่าสภาพแวดล้อม |

**วิธีการบันทึก Parameters:**

```python
# วิธีที่ 1: บันทึกทีละค่า
mlflow.log_param("learning_rate", 0.001)
mlflow.log_param("batch_size", 32)
mlflow.log_param("epochs", 100)

# วิธีที่ 2: บันทึกหลายค่าพร้อมกัน (แนะนำ)
mlflow.log_params({
    "optimizer": "adam",
    "dropout": 0.5,
    "hidden_units": 256,
    "activation": "relu"
})

# วิธีที่ 3: บันทึกจาก argparse หรือ config dict
import argparse
args = argparse.Namespace(lr=0.001, batch=32)
mlflow.log_params(vars(args))
```

**ข้อควรระวัง:**
- Parameter value ต้องเป็น string หรือ number เท่านั้น
- ไม่สามารถแก้ไข Parameter หลังจากบันทึกแล้ว
- ชื่อ Parameter ควรสื่อความหมายและใช้รูปแบบเดียวกันตลอด

#### 4️⃣ Metrics (เมตริก)

**Metrics** คือค่าที่วัดได้จากการทดลอง (Output/Results) สามารถบันทึกได้หลายครั้งพร้อม step number เพื่อติดตามการเปลี่ยนแปลงตามเวลา

**ประเภทของ Metrics:**

| ประเภท | ตัวอย่าง | คำอธิบาย |
|--------|----------|----------|
| **Performance Metrics** | accuracy, loss, f1_score, precision, recall, auc_roc | วัดประสิทธิภาพ Model |
| **Training Metrics** | train_loss, val_loss, train_acc, val_acc | วัดระหว่างการ Train |
| **Time Metrics** | training_time, inference_time, epoch_time | วัดเวลาที่ใช้ |
| **Resource Metrics** | gpu_memory, cpu_usage | วัดการใช้ทรัพยากร |
| **Custom Metrics** | business_metric, custom_score | Metrics ที่กำหนดเอง |

**วิธีการบันทึก Metrics:**

```python
# วิธีที่ 1: บันทึก Metric ค่าเดียว (Final metric)
mlflow.log_metric("accuracy", 0.95)
mlflow.log_metric("f1_score", 0.92)

# วิธีที่ 2: บันทึก Metric ที่เปลี่ยนแปลงตามเวลา
# step parameter ใช้ระบุลำดับ (เช่น epoch number)
for epoch in range(100):
    train_loss, val_loss = train_one_epoch()
    mlflow.log_metric("train_loss", train_loss, step=epoch)
    mlflow.log_metric("val_loss", val_loss, step=epoch)

# วิธีที่ 3: บันทึกหลาย Metrics พร้อมกัน
mlflow.log_metrics({
    "precision": 0.89,
    "recall": 0.94,
    "f1": 0.91
}, step=epoch)

# วิธีที่ 4: บันทึก Metric พร้อม timestamp
mlflow.log_metric("accuracy", 0.95, timestamp=1234567890)
```

**ความแตกต่างระหว่าง Parameters และ Metrics:**

| คุณสมบัติ | Parameters | Metrics |
|-----------|------------|---------|
| **เวลาบันทึก** | ก่อนเริ่มทดลอง | ระหว่าง/หลังทดลอง |
| **จำนวนค่า** | ค่าเดียวต่อชื่อ | หลายค่าได้ (ต่าง step) |
| **การเปลี่ยนแปลง** | คงที่ | เปลี่ยนได้ตามเวลา |
| **วัตถุประสงค์** | บอกว่าทำอะไร (Input) | บอกว่าได้ผลอย่างไร (Output) |
| **ตัวอย่าง** | learning_rate=0.001 | accuracy=0.95 |

#### 5️⃣ Artifacts (สิ่งประดิษฐ์)

**Artifacts** คือไฟล์ที่สร้างจากการทดลอง สามารถเป็นไฟล์ประเภทใดก็ได้ MLflow จะเก็บไว้ใน Artifact Store

**ประเภทของ Artifacts:**

| ประเภท | ตัวอย่าง | คำอธิบาย |
|--------|----------|----------|
| **Models** | model.pkl, model.h5, model.pt, model.onnx | ไฟล์ Model ที่ Train เสร็จ |
| **Visualizations** | confusion_matrix.png, loss_curve.png, roc_curve.png | กราฟและภาพ |
| **Data** | predictions.csv, feature_importance.json, test_results.parquet | ข้อมูลผลลัพธ์ |
| **Code** | train.py, config.yaml, requirements.txt | Source code และ config |
| **Reports** | report.html, summary.pdf | รายงานสรุป |

**วิธีการบันทึก Artifacts:**

```python
import mlflow
import matplotlib.pyplot as plt
import os

# สร้างโฟลเดอร์สำหรับเก็บไฟล์ชั่วคราว
os.makedirs("outputs/models", exist_ok=True)
os.makedirs("outputs/plots", exist_ok=True)
os.makedirs("outputs/data", exist_ok=True)

with mlflow.start_run():
    
    # ----- วิธีที่ 1: บันทึกไฟล์เดี่ยว -----
    # ไฟล์จะถูกเก็บที่ root ของ artifacts/
    mlflow.log_artifact("model.pkl")                    # → artifacts/model.pkl
    mlflow.log_artifact("confusion_matrix.png")         # → artifacts/confusion_matrix.png
    
    # ----- วิธีที่ 2: บันทึกไฟล์ไปยัง subdirectory -----
    # ใช้ artifact_path กำหนดโฟลเดอร์ปลายทาง
    mlflow.log_artifact("model.pkl", artifact_path="models")           # → artifacts/models/model.pkl
    mlflow.log_artifact("predictions.csv", artifact_path="data")       # → artifacts/data/predictions.csv
    
    # ----- วิธีที่ 3: บันทึกทั้งโฟลเดอร์ -----
    # ไฟล์ทั้งหมดในโฟลเดอร์จะถูกบันทึก
    mlflow.log_artifacts("./outputs/plots")                            # → artifacts/[ไฟล์ทั้งหมดใน plots]
    mlflow.log_artifacts("./outputs/plots", artifact_path="visualizations")  # → artifacts/visualizations/[ไฟล์]
    
    # ----- วิธีที่ 4: บันทึก dict เป็น JSON (MLflow 2.0+) -----
    config = {"learning_rate": 0.001, "batch_size": 32}
    mlflow.log_dict(config, artifact_file="config/hyperparameters.json")  # → artifacts/config/hyperparameters.json
    
    # ----- วิธีที่ 5: บันทึก text (MLflow 2.0+) -----
    mlflow.log_text("RandomForest v1.0", artifact_file="models/model_metadata.txt")  # → artifacts/models/model_metadata.txt
    
    # ----- วิธีที่ 6: บันทึก figure จาก matplotlib (MLflow 1.13+) -----
    fig, ax = plt.subplots()
    ax.plot([1, 2, 3], [1, 4, 9])
    ax.set_title("Loss Curve")
    mlflow.log_figure(fig, artifact_file="plots/loss_curve.png")       # → artifacts/plots/loss_curve.png
    plt.close(fig)  # ปิด figure หลังบันทึกเพื่อป้องกัน memory leak
```

**Artifact Organization (ผลลัพธ์ที่ได้จาก Code ด้านบน):**

```
artifacts/
│
├── model.pkl                      ← วิธีที่ 1: log_artifact("model.pkl")
├── confusion_matrix.png           ← วิธีที่ 1: log_artifact("confusion_matrix.png")
│
├── models/
│   ├── model.pkl                  ← วิธีที่ 2: log_artifact("model.pkl", artifact_path="models")
│   └── model_metadata.txt         ← วิธีที่ 5: log_text(..., artifact_file="models/model_metadata.txt")
│
├── data/
│   └── predictions.csv            ← วิธีที่ 2: log_artifact("predictions.csv", artifact_path="data")
│
├── plots/
│   └── loss_curve.png             ← วิธีที่ 6: log_figure(fig, artifact_file="plots/loss_curve.png")
│
├── visualizations/
│   └── [ไฟล์จาก ./outputs/plots]  ← วิธีที่ 3: log_artifacts("./outputs/plots", artifact_path="visualizations")
│
└── config/
    └── hyperparameters.json       ← วิธีที่ 4: log_dict(config, artifact_file="config/hyperparameters.json")
```

**สรุปความแตกต่างของแต่ละวิธี:**

| วิธี | Function | Input | Output Location |
|------|----------|-------|-----------------|
| 1 | `log_artifact(local_path)` | ไฟล์ในเครื่อง | `artifacts/` (root) |
| 2 | `log_artifact(local_path, artifact_path)` | ไฟล์ในเครื่อง | `artifacts/{artifact_path}/` |
| 3 | `log_artifacts(local_dir)` | โฟลเดอร์ในเครื่อง | `artifacts/` หรือ `artifacts/{artifact_path}/` |
| 4 | `log_dict(dict, artifact_file)` | Python dict | `artifacts/{artifact_file}` |
| 5 | `log_text(text, artifact_file)` | Python string | `artifacts/{artifact_file}` |
| 6 | `log_figure(fig, artifact_file)` | Matplotlib figure | `artifacts/{artifact_file}` |

> 💡 **หมายเหตุ**: วิธีที่ 1-3 ใช้ `artifact_path` (ระบุโฟลเดอร์) ส่วนวิธีที่ 4-6 ใช้ `artifact_file` (ระบุ path เต็มรวมชื่อไฟล์)

---

### 📊 ภาพรวมความสัมพันธ์ขององค์ประกอบ

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           MLflow Tracking                               │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                    Experiment: "spam-detection"                   │  │
│  │                                                                   │  │
│  │  ┌─────────────────────────────────────────────────────────────┐  │  │
│  │  │                      Run: run_001                           │  │  │
│  │  │                                                             │  │  │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │  │  │
│  │  │  │ Parameters  │  │   Metrics   │  │     Artifacts       │  │  │  │
│  │  │  │             │  │             │  │                     │  │  │  │
│  │  │  │ • lr=0.001  │  │ • acc=0.92  │  │ • model.pkl         │  │  │  │
│  │  │  │ • epochs=50 │  │ • loss=0.23 │  │ • confusion.png     │  │  │  │
│  │  │  │ • batch=32  │  │ • f1=0.89   │  │ • requirements.txt  │  │  │  │
│  │  │  └─────────────┘  └─────────────┘  └─────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────────────────┘  │  │
│  │                                                                   │  │
│  │  ┌─────────────────────────────────────────────────────────────┐  │  │
│  │  │                      Run: run_002                           │  │  │
│  │  │                         ...                                 │  │  │
│  │  └─────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 🗄️ Backend Store vs Artifact Store

MLflow แยกการเก็บข้อมูลออกเป็น 2 ส่วนที่ชัดเจน เพื่อประสิทธิภาพและความยืดหยุ่นในการ Scale:

```
┌─────────────────────────────────────────────────────────────────┐
│                    MLflow Storage Architecture                  │
├─────────────────────────────────┬───────────────────────────────┤
│         Backend Store           │        Artifact Store         │
│      (เก็บ Metadata)            │      (เก็บไฟล์)               │
├─────────────────────────────────┼───────────────────────────────┤
│  เก็บอะไร?                      │  เก็บอะไร?                    │
│  • Experiment info              │  • Model files                │
│  • Run metadata                 │  • Images/Plots               │
│  • Parameters                   │  • Data files                 │
│  • Metrics                      │  • Any binary files           │
│  • Tags                         │  • Large files                │
├─────────────────────────────────┼───────────────────────────────┤
│  ลักษณะข้อมูล:                  │  ลักษณะข้อมูล:                │
│  • Structured (key-value)       │  • Unstructured (files)       │
│  • Small size                   │  • Variable size              │
│  • Query-able                   │  • Download/Upload            │
├─────────────────────────────────┼───────────────────────────────┤
│  ตัวเลือก:                      │  ตัวเลือก:                    │
│  • File system (mlruns/)        │  • Local filesystem           │
│  • SQLite                       │  • Amazon S3                  │
│  • MySQL/PostgreSQL             │  • Azure Blob Storage         │
│  • Cloud databases              │  • Google Cloud Storage       │
│                                 │  • SFTP                       │
└─────────────────────────────────┴───────────────────────────────┘
```

**ทำไมต้องแยก?**

1. **Performance**: Metadata ถูก query บ่อย ต้องเร็ว ส่วนไฟล์ใหญ่เข้าถึงไม่บ่อย
2. **Scalability**: สามารถ scale แต่ละส่วนแยกกันตามความต้องการ
3. **Cost Optimization**: ใช้ storage ที่เหมาะสมกับประเภทข้อมูล
4. **Flexibility**: เลือก backend ได้ตามโครงสร้างพื้นฐานที่มี

**ใน Lab นี้เราใช้:**
- Backend Store: **SQLite** (ไฟล์ `mlflow.db`) - เหมาะสำหรับการเรียนรู้และทีมขนาดเล็ก
- Artifact Store: **Local filesystem** (โฟลเดอร์ `mlartifacts/`) - เก็บไฟล์ในเครื่อง

**สำหรับ Production:**
- Backend Store: **PostgreSQL** หรือ **MySQL** - รองรับ concurrent access
- Artifact Store: **S3** หรือ **GCS** - ราคาถูก, scalable, durable

---

### 🔄 MLflow Tracking Workflow

Workflow มาตรฐานในการใช้ MLflow Tracking:

```
┌────────────────────────────────────────────────────────────────────────┐
│                      MLflow Tracking Workflow                          │
│                                                                        │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────────────┐ │
│   │  1.SET   │───▶│  2.LOG   │───▶│  3.LOG   │───▶│    4.COMPARE     │ │
│   │EXPERIMENT│    │  PARAMS  │    │ METRICS  │    │    & ANALYZE     │ │
│   └──────────┘    └──────────┘    └──────────┘    └──────────────────┘ │
│        │                                                    │          │
│        │              ┌──────────┐                          │          │
│        │              │  3.LOG   │                          │          │
│        │              │ARTIFACTS │──────────────────────────┘          │
│        │              └──────────┘                                     │
│        │                                                               │
│        ▼                                                               │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                        MLflow Server                            │  │
│   │  ┌─────────────────────┐    ┌────────────────────────────────┐  │  │
│   │  │   Backend Store     │    │        Artifact Store          │  │  │
│   │  │   (SQLite DB)       │    │        (Local/S3/...)          │  │  │
│   │  └─────────────────────┘    └────────────────────────────────┘  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                              │                                         │
│                              ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │     MLflow UI       │                             │
│                    │  http://127.0.0.1   │                             │
│                    │       :8080         │                             │
│                    └─────────────────────┘                             │
└────────────────────────────────────────────────────────────────────────┘
```

**รายละเอียดแต่ละขั้นตอน:**

**Step 1: Set Experiment**
```python
mlflow.set_tracking_uri("http://127.0.0.1:8080")  # เชื่อมต่อ Server
mlflow.set_experiment("my-experiment")            # เลือก Experiment
```

**Step 2: Log Parameters**
```python
with mlflow.start_run():
    mlflow.log_params({
        "model": "RandomForest",
        "n_estimators": 100,
        "max_depth": 10
    })
```

**Step 3: Log Metrics & Artifacts**
```python
    # Training loop
    for epoch in range(epochs):
        loss = train()
        mlflow.log_metric("loss", loss, step=epoch)
    
    # Save artifacts
    mlflow.log_artifact("model.pkl")
    mlflow.log_artifact("confusion_matrix.png")
```

**Step 4: Compare & Analyze**
- ใช้ MLflow UI เปรียบเทียบ Runs
- Query Runs ด้วย Python API
- Export ผลลัพธ์เป็น DataFrame

---

### 🔌 Tracking URI และ Remote Tracking

**Tracking URI** คือที่อยู่ของ MLflow Tracking Server ที่จะบันทึกข้อมูล

**รูปแบบของ Tracking URI:**

```python
# 1. Local file system (default)
mlflow.set_tracking_uri("./mlruns")
# หรือไม่ต้องตั้งค่า จะใช้ ./mlruns อัตโนมัติ

# 2. Local SQLite database
mlflow.set_tracking_uri("sqlite:///mlflow.db")

# 3. Remote HTTP server
mlflow.set_tracking_uri("http://127.0.0.1:8080")
mlflow.set_tracking_uri("https://mlflow.company.com")

# 4. Remote database
mlflow.set_tracking_uri("postgresql://user:pass@host:5432/mlflow")
mlflow.set_tracking_uri("mysql://user:pass@host:3306/mlflow")
```

**ตั้งค่าผ่าน Environment Variable:**
```bash
export MLFLOW_TRACKING_URI=http://127.0.0.1:8080
```

**ข้อดีของ Remote Tracking Server:**
1. **Collaboration**: ทีมทุกคนเห็นผลการทดลองเดียวกัน
2. **Persistence**: ข้อมูลไม่หายเมื่อปิดเครื่อง
3. **Scalability**: รองรับข้อมูลจำนวนมาก
4. **Security**: ควบคุม access ได้

---

### 🏷️ Tags และการจัดระเบียบ

**Tags** คือ key-value pairs ที่ใช้เก็บ metadata เพิ่มเติม แตกต่างจาก Parameters ตรงที่ Tags ไม่ใช่ hyperparameters แต่เป็นข้อมูลเสริมสำหรับการจัดระเบียบและค้นหา

**Use Cases ของ Tags:**

| Tag | ตัวอย่างค่า | วัตถุประสงค์ |
|-----|-------------|--------------|
| `developer` | "john", "team_a" | ระบุผู้ทดลอง |
| `environment` | "development", "production" | ระบุสภาพแวดล้อม |
| `dataset_version` | "v2.1", "2024-01-15" | ระบุ version ของข้อมูล |
| `experiment_type` | "baseline", "ablation", "final" | ประเภทการทดลอง |
| `git_commit` | "abc1234" | ระบุ code version |
| `priority` | "high", "low" | ความสำคัญ |
| `status` | "in_progress", "completed", "failed" | สถานะ |

**วิธีการใช้งาน Tags:**

```python
with mlflow.start_run():
    # ตั้ง Tags ทีละค่า
    mlflow.set_tag("developer", "john")
    mlflow.set_tag("experiment_type", "baseline")
    
    # ตั้ง Tags หลายค่าพร้อมกัน
    mlflow.set_tags({
        "dataset_version": "v2.1",
        "environment": "development",
        "git_commit": "abc1234"
    })
```

**System Tags (ตั้งโดย MLflow อัตโนมัติ):**
- `mlflow.source.name`: ชื่อไฟล์ที่รัน
- `mlflow.source.type`: ประเภท source (LOCAL, NOTEBOOK, PROJECT)
- `mlflow.user`: username ของผู้รัน
- `mlflow.source.git.commit`: Git commit hash (ถ้ารันใน Git repo)

---

### 🎭 Model Registry (ทฤษฎีเบื้องต้น)

> ⚠️ **หมายเหตุ**: ส่วนนี้เป็นทฤษฎีเท่านั้น การปฏิบัติจะอยู่ใน Lab ถัดไป

**Model Registry** คือระบบจัดการเวอร์ชันของ Model แบบรวมศูนย์ เป็น component สำคัญในการนำ Model ไป Production

```
┌─────────────────────────────────────────────────────────────────┐
│                       Model Registry                            │
│                                                                 │
│  Registered Model: "spam-classifier"                            │
│  ├── Version 1 (Stage: Archived)                                │
│  │   └── Source: runs:/abc123/model                             │
│  ├── Version 2 (Stage: Staging)                                 │
│  │   └── Source: runs:/def456/model                             │
│  └── Version 3 (Stage: Production)                              │
│      └── Source: runs:/ghi789/model                             │
│                                                                 │
│  Model Stages:                                                  │
│  ┌─────────┐    ┌─────────┐    ┌────────────┐    ┌──────────┐   │
│  │  None   │───▶│ Staging │───▶│ Production │───▶│ Archived │   │
│  └─────────┘    └─────────┘    └────────────┘    └──────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

**Model Stages อธิบาย:**

| Stage | คำอธิบาย | การใช้งาน |
|-------|----------|-----------|
| **None** | เริ่มต้น ยังไม่ได้กำหนด stage | Model ที่เพิ่งลงทะเบียน |
| **Staging** | กำลังทดสอบ | ทดสอบก่อนขึ้น Production |
| **Production** | ใช้งานจริง | Model ที่ serve อยู่ |
| **Archived** | เลิกใช้แล้ว | เก็บไว้อ้างอิง |

**ประโยชน์ของ Model Registry:**
1. **Version Control**: ติดตามทุก version ของ Model
2. **Stage Management**: รู้ว่า Model ไหนอยู่ใน Production
3. **Lineage Tracking**: รู้ว่า Model มาจาก Run ไหน ใช้ data อะไร
4. **A/B Testing**: เปรียบเทียบ Model versions ได้ง่าย
5. **Easy Rollback**: กลับไป version เก่าได้ทันที
6. **Audit Trail**: มี history ครบถ้วน

---

### 🤖 Autolog - การบันทึกอัตโนมัติ

MLflow รองรับการบันทึกข้อมูลอัตโนมัติสำหรับ ML Frameworks ยอดนิยม ลดการเขียน code ซ้ำซ้อน

**Frameworks ที่รองรับ:**

| Framework | Function | สิ่งที่บันทึกอัตโนมัติ |
|-----------|----------|----------------------|
| **scikit-learn** | `mlflow.sklearn.autolog()` | Parameters, Metrics, Model, Feature names |
| **PyTorch** | `mlflow.pytorch.autolog()` | Parameters, Metrics, Model |
| **TensorFlow/Keras** | `mlflow.tensorflow.autolog()` | Parameters, Metrics per epoch, Model |
| **XGBoost** | `mlflow.xgboost.autolog()` | Parameters, Metrics, Feature importance |
| **LightGBM** | `mlflow.lightgbm.autolog()` | Parameters, Metrics, Feature importance |
| **Spark** | `mlflow.spark.autolog()` | Parameters, Metrics, Model |
| **FastAI** | `mlflow.fastai.autolog()` | Parameters, Metrics, Model |

**ตัวอย่างการใช้งาน Autolog:**

```python
import mlflow
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import train_test_split
from sklearn.datasets import load_iris

# เปิด Autolog ก่อน training
mlflow.sklearn.autolog(
    log_input_examples=True,      # บันทึก input ตัวอย่าง
    log_model_signatures=True,    # บันทึก signature ของ Model
    log_models=True,              # บันทึก Model
    log_datasets=True,            # บันทึก dataset metadata
    silent=False                  # แสดง log messages
)

# Load data
X, y = load_iris(return_X_y=True)
X_train, X_test, y_train, y_test = train_test_split(X, y)

# Training - MLflow บันทึกทุกอย่างอัตโนมัติ!
model = RandomForestClassifier(n_estimators=100, max_depth=5)
model.fit(X_train, y_train)

# ปิด Autolog หลังใช้งาน
mlflow.sklearn.autolog(disable=True)
```

**สิ่งที่ Autolog บันทึกให้ (sklearn):**
- **Parameters**: ทุก hyperparameter ของ Model
- **Metrics**: accuracy, precision, recall, f1 (สำหรับ classification)
- **Artifacts**: Trained model, Confusion matrix, Feature importance plot
- **Model Signature**: Input/output schema

---

### 📐 Best Practices สำหรับ MLflow Tracking

#### 1. การตั้งชื่อ (Naming Conventions)

```
Experiments:  project-name-task         เช่น fraud-detection-xgboost
Runs:         model-variant-date        เช่น resnet50-augmented-20240115
Parameters:   snake_case                เช่น learning_rate, batch_size
Metrics:      snake_case                เช่น val_accuracy, f1_score
Tags:         snake_case                เช่น experiment_type, developer
```

#### 2. การจัดระเบียบ Experiments

```
project-name/
├── baseline/           # Baseline models
├── feature-engineering/# ทดลอง features ใหม่
├── hyperparameter/     # Hyperparameter tuning
├── ablation/           # Ablation studies
└── production/         # Final production models
```

#### 3. การบันทึก Metrics ที่ครบถ้วน

```python
# บันทึกทั้ง training และ validation metrics
mlflow.log_metrics({
    "train_loss": train_loss,
    "val_loss": val_loss,
    "train_accuracy": train_acc,
    "val_accuracy": val_acc
}, step=epoch)

# บันทึก final metrics แยก
mlflow.log_metrics({
    "final_val_accuracy": best_val_acc,
    "best_epoch": best_epoch,
    "total_training_time": training_time
})
```

#### 4. การ Reproduce Experiments

```python
# บันทึกทุกอย่างที่จำเป็นต้องใช้ reproduce
mlflow.set_tag("git_commit", get_git_commit_hash())
mlflow.log_artifact("config.yaml")
mlflow.log_artifact("requirements.txt")

# บันทึก random seeds
mlflow.log_params({
    "random_seed": 42,
    "numpy_seed": 42,
    "torch_seed": 42
})
```

---

## 🔬 Pipeline ภาพรวมของ Lab

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Lab Pipeline Overview                            │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Pre-requisite: MLflow Server ต้องรันอยู่แล้ว                    │    │
│  │ (จาก mlflowserver-lab ที่ http://127.0.0.1:8080)                │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Lab 1: Basic Tracking                                           │    │
│  │ • เรียนรู้การ log_param, log_metric, log_artifact               │    │
│  │ • ดูผลลัพธ์ใน MLflow UI                                         │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Lab 2: Training Loop Tracking                                   │    │
│  │ • บันทึก Metrics ตลอดการ Training                               │    │
│  │ • สร้างกราฟ Loss Curve                                          │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Lab 3: Sklearn Integration                                      │    │
│  │ • ใช้ mlflow.sklearn.autolog()                                  │    │
│  │ • บันทึก Model อัตโนมัติ                                        │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Lab 4: Hyperparameter Comparison                                │    │
│  │ • ทดลองหลาย Hyperparameter combinations                         │    │
│  │ • เปรียบเทียบผลลัพธ์ใน UI                                       │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Lab 5: Custom Artifacts                                         │    │
│  │ • บันทึก Confusion Matrix                                       │    │
│  │ • บันทึก Feature Importance                                     │    │
│  │ • บันทึก Custom Plots                                           │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Lab 6: Query & Search Runs                                      │    │
│  │ • ค้นหา Runs ด้วย MLflow API                                    │    │
│  │ • เปรียบเทียบ Runs โดย Programming                              │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ สรุป: ทบทวนและทำความเข้าใจภาพรวม                                │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## ⚙️ Pre-requisite: เตรียมความพร้อมก่อนเริ่ม Lab

### 📋 สิ่งที่ต้องมี

ก่อนเริ่ม Lab นี้ ตรวจสอบว่าคุณได้ทำสิ่งต่อไปนี้แล้ว:

1. ✅ **MLflow Server** รันอยู่แล้วจาก `mlflowserver-lab`
2. ✅ เข้าถึง MLflow UI ได้ที่ `http://127.0.0.1:8080`

### 🔍 ตรวจสอบ MLflow Server

เปิด Browser แล้วไปที่: [http://127.0.0.1:8080](http://127.0.0.1:8080)

ถ้า Server ยังไม่รัน ให้ไปที่ Terminal ของ `mlflowserver-lab` และรันคำสั่ง:

```bash
# ไปที่โฟลเดอร์ mlflowserver-lab
cd mlflowserver-lab

# เปิดใช้งาน Virtual Environment
source .venv/bin/activate

# รัน MLflow Server
mlflow server \
  --host 127.0.0.1 --port 8080 \
  --backend-store-uri sqlite:///mlruns_db/mlflow.db \
  --artifacts-destination ./mlartifacts \
  --serve-artifacts
```

---

## 🚀 ขั้นตอนการเตรียม Lab Environment